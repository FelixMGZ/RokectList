import 'package:flutter/material.dart';

class AppColors {
  // 1. Colores de Marca
  static const Color primary = Color(0xFF2196F3); // Azul Principal
  static const Color error = Color(0xFFD32F2F);   // Rojo Error
  
  // 2. Fondos
  static const Color background = Color(0xFFF8FAFC); // Gris muy suave (Fondo general)
  static const Color surface = Colors.white;         // Blanco (Tarjetas, Modales, AppBar)
  
  // 3. Textos e Iconos
  static const Color textPrimary = Color(0xFF1E293B);   // Negro suave (Títulos)
  static const Color textSecondary = Color(0xFF64748B); // Gris (Subtítulos)
  
  // 4. Bordes y Elementos UI
  static const Color border = Color(0xFFE2E8F0);        // Gris claro (Bordes de inputs)
  static const Color inputFill = Color(0xFFF1F5F9);     // Gris relleno (Inputs)
}